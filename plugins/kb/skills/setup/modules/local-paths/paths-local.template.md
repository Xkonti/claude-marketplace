## Machine Paths — THIS machine only

Gitignored. Never commit. Missing on fresh clone → recreate per references rule § Missing paths.local.md.

| alias | path |
|-------|------|
| {{kb_alias}} | {{kb_abs_path}} |
<!-- ADAPT[seed-paths]: one row per repo alias w/ known local path on THIS machine. Unknown → omit row (remote-only). -->
